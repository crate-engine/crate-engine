# PROGRESS.md — {{PROJECT}}

> **Fill me in.** This is the live progress tracker that every agent reads on spin-up
> and updates after every step. Delete this banner once the first real entry is in.

## Current State

<!-- One sentence: what phase/loop are we in? What's the overall status? -->

## Done

<!--
  Reverse-chronological. Each entry: date, what was done, branch/commit, key decisions.
  Example:
    - 2026-06-18: Added 9 GSC-recovery 301 redirects to next.config.js (branch feature/redirects-gsc-recovery, merged)
    - 2026-06-18: Fixed import calculator crash — removed stale Port Handling row, null-safe fmt() (branch fix/import-calc-crash-port-handling, merged)
-->

## Next

<!--
  Ordered by priority. Each item: what, which agent leads, approximate scope.
  Example:
    - Wire up inventory page to R2 data (Coder, ~2 steps)
    - Design contact page (Designer → Coder, ~3 steps)
-->

## Locked Decisions

<!--
  Decisions that should NOT be revisited without explicit discussion.
  Example:
    - npm only (never pnpm — package-lock.json is canonical)
    - Tailwind utility classes only (no CSS var arbitrary values, except rgba() shadows)
    - Vercel auto-deploys on main push — no separate deploy step
-->
